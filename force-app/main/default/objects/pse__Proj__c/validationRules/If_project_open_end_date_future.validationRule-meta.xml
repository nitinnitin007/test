<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>If_project_open_end_date_future</fullName>
    <active>false</active>
    <description>If a project is open, the end date must be in the future.</description>
    <errorConditionFormula>AND(
  CONTAINS(TEXT(pse__Stage__c),&quot;Open&quot;),
  pse__End_Date__c&lt;TODAY()
)</errorConditionFormula>
    <errorDisplayField>pse__End_Date__c</errorDisplayField>
    <errorMessage>If the project is open, please make sure that the end date is in the future.</errorMessage>
</ValidationRule>
