<?xml version="1.0" encoding="UTF-8"?>
<Workflow xmlns="http://soap.sforce.com/2006/04/metadata">
    <rules>
        <fullName>Rule Code%3A OPC-001xFxx</fullName>
        <actions>
            <name>Populate_Recurring_Amount_Change_Update</name>
            <type>FieldUpdate</type>
        </actions>
        <active>true</active>
        <description>Rule Code: OPC-001xFxx 
Populate Recurring Amount Change Update
Updates the recurring amount change update field when the recurring amount change field is updated.</description>
        <formula>OR(
  ISCHANGED(Recurring_Amount_Change__c),
  ISNEW()
)</formula>
        <triggerType>onAllChanges</triggerType>
    </rules>
</Workflow>
